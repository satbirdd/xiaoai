<%= form_for(@line_property_item) do |f| %>
  <% if @line_property_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_property_item.errors.count, "error") %> prohibited this line_property_item from being saved:</h2>

      <ul>
      <% @line_property_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :line_property %><br>
    <%= f.text_field :line_property %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
