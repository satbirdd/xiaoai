<p id="notice"><%= notice %></p>

<p>
  <strong>Targetable:</strong>
  <%= @line_property.targetable_id %>
</p>

<p>
  <strong>Targetable type:</strong>
  <%= @line_property.targetable_type %>
</p>

<p>
  <strong>Name:</strong>
  <%= @line_property.name %>
</p>

<p>
	<strong>Items</strong>
	<% @line_property.line_property_items.each do |item| %>
		<span><%= item.name %></span>
	<% end %>
</p>

<%= link_to 'Edit', edit_line_property_path(@line_property) %> |
<%= link_to 'Back', category_line_properties_path(@line_property.targetable) %>
